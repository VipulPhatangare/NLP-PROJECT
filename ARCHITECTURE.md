# 📊 System Architecture Diagram

## 🎯 Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                              │
│                     (Browser - localhost:3000)                      │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Dashboard (index.html + styles.css + app.js)                │ │
│  │                                                              │ │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────┐│ │
│  │  │  Overview  │ │  Phase 1   │ │  Phase 2   │ │   Data   ││ │
│  │  │    Tab     │ │    Tab     │ │    Tab     │ │   View   ││ │
│  │  └────────────┘ └────────────┘ └────────────┘ └──────────┘│ │
│  │                                                              │ │
│  │  📊 Charts (Chart.js) | 📋 Tables | 🎨 Visualizations       │ │
│  └──────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                  ↕ REST API
┌─────────────────────────────────────────────────────────────────────┐
│                       EXPRESS SERVER (server.js)                    │
│                          Node.js + Express                          │
│                                                                     │
│  API Endpoints:                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  GET  /api/status           → Check data availability        │ │
│  │  POST /api/scrape           → Trigger scraping               │ │
│  │  POST /api/phase1           → Run preprocessing              │ │
│  │  POST /api/phase2           → Run analysis                   │ │
│  │  GET  /api/data/raw         → Fetch raw data                 │ │
│  │  GET  /api/data/cleaned     → Fetch cleaned data             │ │
│  │  GET  /api/results/phase2   → Fetch analysis results         │ │
│  └──────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                  ↕ Python Scripts
┌─────────────────────────────────────────────────────────────────────┐
│                        PYTHON PROCESSING LAYER                      │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  scraper.py                                                │   │
│  │  🕷️  Web Scraping                                          │   │
│  │  • AsyncHTMLSession                                        │   │
│  │  • Batch processing (15 pages/batch)                       │   │
│  │  • Rate limiting (60s delay)                               │   │
│  │  • BeautifulSoup parsing                                   │   │
│  │  Output: data/flipkart_boat_raw.csv                        │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  phase1_preprocessing.py                                   │   │
│  │  🧹 Data Preprocessing                                     │   │
│  │  • Emoji → Word mapping (50+ emojis)                       │   │
│  │  • Language detection (langdetect)                         │   │
│  │  • Translation (deep-translator)                           │   │
│  │  • Text cleaning & normalization                           │   │
│  │  • Tokenization (NLTK)                                     │   │
│  │  • Stopword removal                                        │   │
│  │  • Lemmatization (WordNetLemmatizer)                       │   │
│  │  • Deduplication                                           │   │
│  │  Output: data/flipkart_boat_cleaned.csv                    │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  phase2_analysis.py                                        │   │
│  │  📊 NLP Analysis                                           │   │
│  │  • POS Tagging (NLTK)                                      │   │
│  │  • Named Entity Recognition (rule-based)                   │   │
│  │  • Bag-of-Words (CountVectorizer)                          │   │
│  │  • TF-IDF (TfidfVectorizer)                                │   │
│  │  • LSA Topic Modeling (TruncatedSVD)                       │   │
│  │  • Word2Vec Embeddings (Gensim)                            │   │
│  │  • Semantic Similarity (Cosine)                            │   │
│  │  Output: results/phase2_results.json                       │   │
│  └────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                  ↕ File I/O
┌─────────────────────────────────────────────────────────────────────┐
│                          DATA STORAGE LAYER                         │
│                                                                     │
│  ┌──────────────┐  ┌──────────────────┐  ┌────────────────────┐  │
│  │   data/      │  │     results/     │  │    config.py       │  │
│  │              │  │                  │  │                    │  │
│  │  • raw.csv   │  │  • phase2.json   │  │  • Settings        │  │
│  │  • cleaned   │  │                  │  │  • Parameters      │  │
│  └──────────────┘  └──────────────────┘  └────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

```
┌─────────────┐
│  START      │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  User clicks "Scrape Reviews"           │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  POST /api/scrape                       │
│  → Execute scraper.py                   │
│  → Fetch 60 pages (batches of 15)      │
│  → 60s delay between batches           │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  data/flipkart_boat_raw.csv             │
│  Columns: Name, Title, Rating, Review   │
│  ~600-1000 reviews                      │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  User clicks "Run Phase 1"              │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  POST /api/phase1                       │
│  → Execute phase1_preprocessing.py      │
│  → Emoji mapping                        │
│  → Language detection                   │
│  → Translation (if needed)              │
│  → Text cleaning                        │
│  → Tokenization & lemmatization         │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  data/flipkart_boat_cleaned.csv         │
│  + language                             │
│  + Translated_Review                    │
│  + Cleaned_Review                       │
│  + Processed_Review                     │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Frontend displays:                     │
│  • Language distribution chart          │
│  • Preprocessing stats                  │
│  • Sample processed reviews             │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  User clicks "Run Phase 2"              │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  POST /api/phase2                       │
│  → Execute phase2_analysis.py           │
│  → POS tagging                          │
│  → NER extraction                       │
│  → TF-IDF vectorization                 │
│  → LSA topic modeling                   │
│  → Word2Vec training                    │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  results/phase2_results.json            │
│  {                                      │
│    pos_analysis: {...},                 │
│    ner_analysis: {...},                 │
│    bow_tfidf: {...},                    │
│    lsa_topics: {...},                   │
│    word2vec_similarities: {...}         │
│  }                                      │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Frontend displays:                     │
│  • POS distribution chart               │
│  • Top adjectives/verbs charts          │
│  • NER entities grid                    │
│  • LSA topics with keywords             │
│  • Word2Vec similarity tables           │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  END        │
└─────────────┘
```

## 📊 Visualization Architecture

```
Chart.js Visualizations
├── Pie Chart
│   └── Language Distribution
│       └── Data: {en: 450, hi: 120, mr: 30, ...}
│
├── Bar Charts (Vertical)
│   └── POS Tag Distribution
│       └── Data: {NN: 1500, VB: 800, JJ: 600, ...}
│
├── Bar Charts (Horizontal)
│   ├── Top Adjectives
│   │   └── Data: {good: 150, best: 120, great: 100, ...}
│   └── Top Verbs
│       └── Data: {work: 80, recommend: 60, buy: 50, ...}
│
└── Custom Visualizations
    ├── NER Entity Cards
    │   └── Grid layout with counts
    ├── Topic Cards
    │   └── Keywords with variance
    └── Similarity Grids
        └── Term → Similar words with scores
```

## 🎨 UI Component Hierarchy

```
Dashboard (index.html)
│
├── Header
│   ├── Title: "🎯 NLP Product Review Analysis"
│   ├── Subtitle
│   └── Author
│
├── Status Bar
│   ├── Raw Data Status
│   ├── Phase 1 Status
│   └── Phase 2 Status
│
├── Control Panel
│   ├── Scrape Reviews Button
│   ├── Run Phase 1 Button
│   ├── Run Phase 2 Button
│   └── View All Data Button
│
├── Loading Indicator
│   └── Spinner + Text
│
└── Tabbed Interface
    │
    ├── Overview Tab
    │   └── Stats Grid
    │       ├── Total Reviews Card
    │       ├── Language Count Card
    │       ├── Topic Count Card
    │       └── Vocabulary Size Card
    │
    ├── Phase 1 Tab
    │   ├── Language Chart Section
    │   ├── Stats Table Section
    │   └── Sample Reviews Section
    │
    ├── Phase 2 Tab
    │   ├── POS Chart Section
    │   ├── Top Words Section (2 columns)
    │   ├── NER Results Section
    │   ├── Topics Section
    │   └── Word2Vec Section
    │
    └── Data View Tab
        ├── Data Type Selector
        └── Data Table (dynamic)
```

## 🔧 Technology Stack Diagram

```
Frontend Layer
├── HTML5
│   └── Semantic markup, accessibility
├── CSS3
│   ├── Flexbox layouts
│   ├── Grid layouts
│   ├── CSS variables
│   └── Responsive design
└── JavaScript (ES6+)
    ├── Async/await
    ├── Fetch API
    ├── Chart.js
    └── DOM manipulation

Backend Layer
├── Node.js 14+
│   └── JavaScript runtime
└── Express 4.x
    ├── Routing
    ├── Middleware (CORS, JSON)
    ├── Static file serving
    └── Child process spawning

Processing Layer
├── Python 3.8+
│   └── Data processing runtime
├── Web Scraping
│   ├── requests-html
│   ├── beautifulsoup4
│   └── nest-asyncio
├── NLP Libraries
│   ├── NLTK (tokenization, POS, lemma)
│   ├── Gensim (Word2Vec)
│   └── scikit-learn (TF-IDF, LSA)
├── Data Libraries
│   ├── pandas
│   └── numpy
└── Translation
    ├── deep-translator
    └── langdetect

Storage Layer
└── File System
    ├── CSV files (data/)
    └── JSON files (results/)
```

---

**This diagram is for reference only - the actual system is fully implemented and ready to use!**
